{
  "name": "json-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "json-server db.json --watch -m ./node_modules/json-server-auth -r ./routes.json",
    "auth": "json-server-auth db.json --watch -r routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server-auth": "^2.1.0"
  },
  "devDependencies": {
    "json-server": "^0.17.1"
  }
}
